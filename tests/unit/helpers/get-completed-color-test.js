import { getCompletedColor } from 'to-do-front/helpers/get-completed-color';
import { module, test } from 'qunit';

module('Unit | Helper | get completed color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getCompletedColor([42]);
  assert.ok(result);
});
